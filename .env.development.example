# Optional: copy to .env.development for profile-specific overrides.
# Recommended: use a single repo-root `.env` (see README) — it is preferred for local dev.
PORT=5000
DATABASE_URL=postgresql://YOUR_USER@localhost:5432/lumina
JWT_SECRET=your-random-secret
JWT_ACCESS_EXPIRES_IN=7d
FRONTEND_URL=http://localhost:5173

VITE_API_URL=http://localhost:5000
VITE_API_PREFIX=/api/v1
